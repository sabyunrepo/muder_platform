# PR-164 4-agent Review (Round 1)

**PR:** [#164](https://github.com/sabyunrepo/muder_platform/pull/164) `fix/ci-infra-recovery`
**Date:** 2026-04-28
**Commits reviewed:** 39a56fe → 8d4135d (6 commits)

## Severity 종합

| 영역 | CRITICAL | HIGH | MEDIUM | LOW |
|---|---|---|---|---|
| Security (opus) | 0 | 0 | 0 | 7 |
| Performance (sonnet) | 0 | 1 | 2 | 3 |
| Architecture (opus) | 0 | 0 | 3 | 4 + 4 suggestions |
| Test coverage (sonnet) | 0 | 1* | 3 | 4 |
| **Total** | **0** | **2** | **8** | **18** |

*Test HIGH은 검증 결과 **거짓 양성** — phase-18.1/flaky-report는 `ubuntu-latest` cloud runner라 충돌 없음.

## 머지 차단 판단

**HIGH 진짜 = 1건 (Perf F-1 chown -R /go).** Dev image rebuild 시 20-60s 추가 + 50-200MB layer 비용. 그러나 **CI 영향 0** (CI는 prod `Dockerfile`만 사용). 머지 차단 아님.

**Architecture IMPORTANT 3건** = 머지 전 fold-in 가치 있음 (총 5-10 LOC).

## 영역별 상세

### Security (opus, 0 blocker)
모두 LOW — pre-existing 또는 defense-in-depth nit. PR이 net hardening (Dockerfile.dev appuser 도입). 후속 PR 권고:
- 25432/26379 dev port를 `127.0.0.1:25432:5432` loopback bind (langfuse도 동일 패턴)
- `HOST_UID=0` 거부 가드 (rare)
- CI postgres password를 `secrets.CI_PG_PASSWORD`로 (marginal)

### Performance (sonnet, 1 HIGH dev-only)
- **F-1 HIGH:** `chown -R /go` 4.8GB 트리 walk + 50-200MB layer delta, 20-60s 빌드 시간 증가. **scope = `Dockerfile.dev`만, CI 영향 0**. 권장: `/go/bin`만 chown 또는 `GOBIN` 사용자 홈 분리. 후속 PR 권고.
- **F-2 MEDIUM:** Makefile `up` 타겟에 `HOST_UID=$(id -u)` 자동 주입 안됨 → 사용자가 매번 export 필요. (확인: Makefile에 `up` 타겟 부재)
- **F-3 MEDIUM:** `e2e-stubbed.yml` Go 1.24 vs `ci.yml` 1.25 — pre-existing drift. 별도 PR.

### Architecture (opus, 3 IMPORTANT, 0 blocker)
- **I-1 IMPORTANT:** `refs/task-1-2-dev-compose.md`이 `5432:5432` 그대로 — 실제 commit `8d4135d`은 `25432:5432`. plan refs drift. **머지 전 fix 권장 (1줄)**.
- **I-2 IMPORTANT:** HOST_UID/HOST_GID 기본값 `1000`이 4곳에 분산 (Dockerfile.dev ARG, compose 2곳, CLAUDE.md). Coupling comment 1줄 추가.
- **I-3 IMPORTANT:** `checklist.md` 모든 `- [ ]` 미체크 (Task 1-5 완료에도). **머지 commit에 포함 권장**.

### Test (sonnet, 1 HIGH false-positive)
- **HIGH 거짓 양성:** phase-18.1 + flaky-report는 `ubuntu-latest` cloud → 5432 충돌 없음 → 변경 불필요. **무시**.
- **F-2 MEDIUM:** runner 검증 결과(이미지 build OK + sabyun owner) PR body에만 — memory에 영구화 안 됨. checklist 체크 + memory note 권장.
- **F-3, F-4 MEDIUM:** HOST_UID=0 / Mac UID 501 fallback 자동 테스트 부재. carry-over.
- **F-5 LOW:** `ci.yml` redis service에 langfuse 회피 comment 누락 (postgres만 있음). 1줄 추가 권장.
- **F-8 LOW:** `actionlint` CI step 부재. PR-11 hygiene carry-over.

## 머지 전 권장 액션 (5-10 LOC, 단일 commit)

1. **(I-1)** `refs/task-1-2-dev-compose.md` Step 2.1에 drift note 추가 (25432/26379 shift 명시)
2. **(I-2)** `docker-compose.dev.yml` `${HOST_UID:-1000}` 라인 위에 "Defaults must match Dockerfile.dev ARG USER_UID/USER_GID" comment
3. **(I-3)** `checklist.md` Task 1-5 박스 체크
4. **(F-5 LOW)** `ci.yml` redis ports 위에도 ephemeral comment

## Carry-over (PR-11 hygiene 또는 별도 PR)

- (Sec LOW 1) dev compose `127.0.0.1:25432` loopback bind
- (Sec LOW 2) `HOST_UID=0` 거부 가드
- (Perf F-1 HIGH) `chown -R /go` 스코프 축소 (dev-only impact)
- (Perf F-2) Makefile `up` 타겟에 `HOST_UID` 자동 주입
- (Perf F-3) `e2e-stubbed.yml` Go 1.25 align
- (Test F-8) actionlint CI step
- (Arch S-3) `memory/project_dev_port_convention.md`
- (Arch M-4) `feedback_ci_infra_debt.md` outdated marker 제거 + 본 PR 참조

## 결정 게이트

본 PR은 **HIGH 0 (실질) → 머지 가능**. 단, IMPORTANT 3건 fold-in 권장 (5-10 LOC). 사용자 결정:
- (a) IMPORTANT 3건 fold-in 후 머지 ⭐ 권장
- (b) 그대로 머지 + carry-over 등록
- (c) 모든 IMPORTANT/MED/LOW 한 번에 (scope 너무 ↑)
